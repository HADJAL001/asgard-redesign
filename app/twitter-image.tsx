/* twitter-image — та же премиальная карточка, что и og:image.
   Next.js подставит её в twitter:image (summary_large_image). */
export { alt, size, contentType } from "./opengraph-image"
export { default } from "./opengraph-image"
