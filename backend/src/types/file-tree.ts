/* ================================================================
   OSGARD · Общий тип файлового дерева
   ----------------------------------------------------------------
   Единое представление набора файлов сгенерированного проекта,
   используемое всеми интеграционными адаптерами (Vercel, GitHub,
   Stripe/Supabase-кодогенерация). Совпадает по форме с
   GeneratedAppFile (app-generator.ts), чтобы результат генератора
   приложений можно было передавать в адаптеры без преобразования.
   ================================================================ */

export type FileTreeEntry = {
  path: string
  content: string
}

export type FileTree = FileTreeEntry[]
