# 🤝 COORDINATION — протокол для двух Клодов на одном репозитории

Над OSGARD работают **два инстанса Claude параллельно**. Этот файл — единственный
источник правды о том, кто что делает. **Читай его ПЕРЕД началом работы и обновляй
свою строку статуса, когда берёшь/заканчиваешь задачу.** Без этого мы затираем труд
друг друга (уже ловили: коллизия миграций, конкурентные правки `auth.controller.ts`).

---

## Роли (зоны ответственности)

| | Claude A | Claude B |
|---|---|---|
| **Домен** | Безопасность/auth, воронки/конверсия, тесты, ops/деплой, гигиена | Монетизация, маркетплейс, аукционы/дропы, генерация/Кузница, real-time |
| **Основные файлы** | `lib/refresh-tokens`, `services/auth.service`, `services/twofa`, `middleware/*`, `routes/auth`, `routes/demo`, `lib/demo-client`, `lib/auth-store` | `routes/tcmarket`, `routes/marketplace`, `routes/artifacts`, `routes/orchestrator`, генерация, `components/Generation*`, платёжки |

Если задача выходит за твой домен — **сначала запиши в статус ниже**, потом делай.

---

## Правила (обязательные)

1. **Никогда не коммить прямо в `main`.** Только feature-ветка → PR → squash-merge.
2. **Перед началом:** `git fetch origin main && git rebase origin/main` на своей ветке.
   Это ловит конфликты рано, а не при мерже.
3. **Миграции — главный источник коллизий.** Перед созданием:
   - `git fetch origin main`, посмотреть максимальный номер в `backend/src/migrations/`;
   - взять **следующий свободный** номер;
   - **сразу вписать его в «Занятые номера миграций» ниже**, чтобы второй не взял тот же.
4. **Общие файлы** (`auth.controller.ts`, `server.ts`, `lib/auth-store.tsx`, `db.ts`) —
   правь только точечно и объяви в статусе, что их трогаешь. Не переписывай целиком.
5. **Мелкие частые коммиты** и быстрый мерж — чем дольше живёт ветка, тем больше
   расхождение. Не копи большие диффы.
6. **После мержа** — обнови статус (задача → done) и, если менял общий файл, напиши это.

---

## 📌 Занятые номера миграций (обновлять при создании!)
Максимум на `main`: **078** (`078_artifact_fusion` — Claude A). Следующая свободная — **079**.
Прежде чем создать `0XX_*.ts`, проверь `main` и увеличь это число здесь.

---

## 📋 Статус в реальном времени (обновляй свою строку)

| Кто | Сейчас делает | Ветка | Файлы/область | Обновлено |
|---|---|---|---|---|
| Claude A | Артефакт-Фьюжн (миграция 078, POST /artifacts/fuse, lib/artifact-fusion, FusionModal). Ранее: security-контур, воронка #35, daily-стрик #37, реф-фикс #38, push-стрик #39 | feature/artifact-fusion | migrations/078, lib/artifact-fusion, routes/artifacts, components/FusionModal, artifacts-view, osgard-store | 2026-07-25 |
| Claude B | (впиши сам) Фаза 3 «снаряжение Кузницы» / монетизация | — | генерация, artifacts | — |

---

## Известное состояние (на 2026-07-25)
- **Прод:** Railway `asgard-backend` (из `main`, root `/backend`), Vercel фронт. Секреты
  и `NODE_ENV=production` заданы. Security-контур в бою и проверен вживую.
- **Воронка demo→register→convert** замкнута (#35): `/demo/convert` вызывается из всех
  auth-путей через `lib/auth-store` → флаг `osgard_demo_converted` → баннер на дашборде.
- **Гигиена:** утёкшие токены (`ghp_`, `EXPO_TOKEN`, Netlify `nfp_`) — отозвать на
  соответствующих сайтах (действие пользователя).
