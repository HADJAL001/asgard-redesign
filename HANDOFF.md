# HANDOFF — OSGARD Mobile / asgard-redesign

Координационный канал между агентами, работающими над этим репозиторием.
Проект отдельный от «Оптимизатора дня» — не смешивать статусы.

## Статус (24.07.2026)

- ✅ PR #4 и PR #6 смержены в `main`
- ✅ Конфликт в `mobile/app/(tabs)/index.tsx` разрешён
- ✅ CI (`mobile-ci.yml`) исправлен — триггерится и на `push` в `main`, не только на PR
- ✅ `main` собирается (`npm run build`), деплой на Vercel/Railway зелёный
- ✅ Продакшен (`https://asgard-redesign.vercel.app`) отвечает корректно на ключевых страницах
- ✅ Detox e2e CI (`e2e-android` job, testID в `login.tsx`) и лимитные ордера
  (`OrderBook.tsx`, `MyOrders.tsx`, переключатель Рынок/Лимит в `wallet.tsx`) —
  уже в `main` (пришли с PR #6), отдельная реализация не требуется
- ✅ Полная верификация `main` (изолированный worktree, коммит `7544561`):
  - root: `npm run build` — ОК, `npm run lint` — 0 ошибок (в root нет `npm run test`)
  - `mobile/`: `tsc --noEmit` — 0 ошибок; `lint` — 0 ошибок (18 преэкзистирующих
    warnings); `jest` — 36/36 тестов пройдено
  - `backend/`: `tsc build` — ОК; `npm test` — 40/47 с первого прогона, 5 тестов
    (`orchestrator-flow.test.ts`, `walli-buy-atomicity.test.ts`) падают по таймауту
    при полном параллельном прогоне (конкуренция за CPU между несколькими
    поднимаемыми tsx-серверами); при изолированном запуске этих двух файлов —
    5/5 пройдено. Не регрессия, а флейк тест-раннера под нагрузкой.
  - деплой: 200 OK, страница рендерится, 401 в консоли — ожидаемо для
    неавторизованного гостя (`/api/wallet`, `/api/stakes` и т.п.)

## ⏳ В ожидании

- Удаление ветки `feature/status-psychology` — на момент проверки активна:
  на 17+ коммитов впереди `origin`, плюс незакоммиченный файл в рабочем дереве
  (кто-то ещё работает в общей директории). Удалять после подтверждения, что
  работа завершена и перенесена в `main`.

## Следующие задачи — статус на момент проверки (24.07.2026, повторно)

Все 4 задачи оказались уже реализованы другими агентами раньше, чем до них
дошла очередь — проверено по коду и `git log`, не с чужих слов:

1. ✅ Sentry + ErrorBoundary — `mobile/lib/sentry.ts`, `mobile/components/ErrorBoundary.tsx`,
   подключен в `app/_layout.tsx` (коммит `41ff12d`)
2. ⚠️ EAS projectId для Push — код готов (`mobile/lib/push.ts` читает
   `Constants.expoConfig.extra.eas.projectId`, есть guard на случай отсутствия),
   но в `mobile/app.json` до сих пор placeholder `"YOUR_EAS_PROJECT_ID"`
   (коммит `2365ae2`). Получить реальный ID может только владелец Expo-аккаунта
   через `eas login` + `eas init` — агент это сделать не может (нет доступа
   к аккаунту/креды). **Единственный реально открытый пункт из четырёх.**
3. ✅ Экран настроек пользователя — `mobile/app/settings.tsx` (push, reduce
   motion, биометрия, смена пароля), коммит `3167c0e`
4. ✅ Тесты — `mobile/lib/__tests__/economy.test.ts`,
   `marketplace-api.test.ts`, `prefsStore.test.ts`, коммит `ded3f0f`.
   Повторный прогон `npx jest --watchAll=false`: **36/36 пройдено**.

## Рекомендация по координации

Множественные агенты правили этот репозиторий параллельно без координации,
из-за чего состояние PR/веток несколько раз расходилось с реальностью в
течение сессии. Чтобы это не повторялось:

- Один ответственный за мержи в `main`
- Все PR проходят через него
- Этот файл — основной канал коммуникации по статусу
- Ветки удаляются сразу после мержа
